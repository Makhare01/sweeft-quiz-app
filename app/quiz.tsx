import { Link } from "expo-router";
import { Text, View } from "react-native";

const QuizView = () => {
  return (
    <View>
      <Text>Quiz View</Text>
      <Link href="/">Home</Link>
    </View>
  );
};

export default QuizView;
